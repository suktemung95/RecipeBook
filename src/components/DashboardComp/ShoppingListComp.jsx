export default function ShoppingListComp({ navItems, currentBoard }) {
    return (
        <div>
            <h2>
                This is the main dashboard area for board: {currentBoard}
            </h2>
        </div>
    )
}