export default function MyRecipesComp({ navItems, currentBoard }) {
    return (
        <div>
            <h2>
                This is the main dashboard area for board: {currentBoard}
            </h2>
        </div>
    )
}